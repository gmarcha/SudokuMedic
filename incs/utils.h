#ifndef UTILS_H
# define UTILS_H

#include <stdio.h>
#include <math.h>
#include "defines.h"
#include "types.h"

void printErr( const string errMsg );
// Utility function to print formatted error.

void printGrid( gridPtr grid );
// Utility function to print sudoky grid.

#endif // UTILS_H
